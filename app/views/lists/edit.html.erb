<!--編集用viewファイル-->
<h1>編集画面</h1>

<%= form_with model: @list do |f| %>
<!--受け取ったインスタンス変数をここでは「f」とする(このView専用の変数にする)-->
  <h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>本文</h4>
  <%= f.text_area :body %>

  <%= f.submit '保存' %>

<% end %>